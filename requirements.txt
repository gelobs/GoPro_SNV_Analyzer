# requirements.txt
# Instale DEPOIS do conda install geopandas pyogrio shapely pyproj
# Comando: pip install -r requirements.txt

# GPS / vídeo GoPro (compatível Python 3.13)
gopro2gpx>=0.1.5
gpxpy>=1.6.2
ffmpeg-python>=0.2.0

# Dados e ML
pandas>=2.2.3
numpy>=2.1.0
scipy>=1.14.1
scikit-learn>=1.5.2

# GIS (já instalado via conda — mantido aqui como referência)
# geopandas>=1.1.0
# pyogrio>=0.10.0
# shapely>=2.0.4
# pyproj>=3.6.1

# Visualização
folium>=0.17.0
matplotlib>=3.9.2
geodatasets>=2023.12.0

flask>=3.0.0
